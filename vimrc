" Indentation settings
set autoindent      " Maintain indentation from the previous line
set smartindent     " Automatically insert indentation in code blocks

" Convert tabs to spaces
set expandtab      " Use spaces instead of tabs
set tabstop=4      " A tab appears as 4 spaces
set shiftwidth=4   " Indent/outdent by 4 spaces
set softtabstop=4  " Number of spaces inserted when pressing Tab

" Enable clipboard support (uncomment if needed)
" set mouse=a       " Enable mouse support for selection and scrolling
" set clipboard=unnamedplus  " Use system clipboard
